---
status: draft   # draft → approved
updated: <YYYY-MM-DD>
related: agent_docs/project/plan.md
---

# 仕様書: <プロジェクト名>

この仕様書は **2層で育てる**（詳細は `agent_docs/common/spec_format.md`）。
plan（どう作るか）とは役割を分け、ここには **何を満たすか（I/O 契約と振る舞い）** だけを書く。

- **Phase 0 で書く（必須・全体を確定）**: 下記「システム全体の入出力」と「モジュール間インターフェース（境界の契約）」、および各機能の **目的 / 完了の目安**。
- **各 Phase の着手時に書く（その Phase 分だけ）**: その Phase で実装する **関数レベルの入力・出力 / 振る舞い**。

---

## システム全体の入出力（Phase 0 で確定）

システムの外部境界。外から見た入力と出力。

| 入力 | 型 | → | 出力 | 型 |
|------|----|---|------|----|
| <input> | <type> | → | <output> | <type> |
| param: <name> | <type> |  |  |  |

## モジュール間インターフェース / 境界の契約（Phase 0 で確定）

Phase（モジュール）をまたいで受け渡すデータの **公開 I/O**。後続 Phase がここに依存する。
ここを Phase 0 で固めることで、Phase 同士が統合時に噛み合う。

| 提供する Phase / モジュール | 受け取る Phase / モジュール | データ | 型 | 意味・制約 |
|------|------|------|----|----|
| <Phase 1> | <Phase 3> | <name> | <type> | <意味・単位・前提> |

---

## 機能1: <名前>（plan の Phase と 1:1）

**目的**: <なぜ必要か。1行>　← *Phase 0 で記入*

**完了の目安**: <「こうなれば動いている」と言える具体例を1〜3個>　← *Phase 0 で粗く記入*

**入力 / 出力（関数レベル）**: ← *この機能を実装する Phase の着手時（plan mode）に詳細化*
| 入力 | 型 | → | 出力 | 型 |
|------|----|---|------|----|
| <topic / param / 引数> | <type> | → | <戻り値 / topic> | <type> |

**振る舞い**: <入力をどう出力へ変えるか。2〜4行>　← *この Phase の着手時に詳細化*

---

## Open Questions

- <未確定事項>
